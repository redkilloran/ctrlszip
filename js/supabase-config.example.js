// Copy this file to supabase-config.js and fill in the values from your
// Supabase project (Project Settings > API in the dashboard).
// supabase-config.js is gitignored — it's fine for these values to be
// "public" (the anon key is designed to be exposed in client code; real
// protection comes from Row Level Security policies on your tables), but
// keeping it out of git avoids churn if you ever recreate the project.

export const SUPABASE_URL = "https://PASTE_ME.supabase.co";
export const SUPABASE_ANON_KEY = "PASTE_ME";
