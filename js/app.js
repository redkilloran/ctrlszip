import { supabase } from "./supabaseClient.js";

// TODO: home page — load and render event folders sorted by activity,
// excluding unlisted/private ones.
console.log("CTRLS.zip booted.", supabase);
